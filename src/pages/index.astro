<!doctype html>
<html lang="en-IN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>StudentKhata Blog | Free Resources for Tuition Teachers</title>
    <meta name="description" content="Free Excel templates, fee register formats, attendance register formats, and guides for tuition teachers and coaching centers." />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://blog.studentkhata.online/" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="StudentKhata Blog | Free Resources for Tuition Teachers" />
    <meta property="og:description" content="Free Excel templates, fee register formats, attendance register formats, and guides for tuition teachers and coaching centers." />
    <meta property="og:url" content="https://blog.studentkhata.online/" />
    <meta property="og:site_name" content="StudentKhata Blog" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,300;0,9..144,700;0,9..144,900;1,9..144,300;1,9..144,700&family=DM+Sans:wght@400;500;600&display=swap" rel="stylesheet" />

    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      html { scroll-behavior: smooth; }

      :root {
        --purple:       #7c3aed;
        --purple-dark:  #5b21b6;
        --purple-deep:  #3b0764;
        --purple-pale:  #f5f3ff;
        --purple-mid:   #ede9fe;
        --purple-glow:  rgba(124, 58, 237, 0.15);
        --ink:          #0f0a1e;
        --ink-mid:      #3d3560;
        --ink-soft:     #6b63a0;
        --ink-muted:    #a89ec8;
        --white:        #ffffff;
        --border:       rgba(124,58,237,0.15);
        --serif:        'Fraunces', Georgia, serif;
        --sans:         'DM Sans', system-ui, sans-serif;
      }

      body {
        font-family: var(--sans);
        background: var(--white);
        color: var(--ink);
        -webkit-font-smoothing: antialiased;
        overflow-x: hidden;
      }

      /* ── NOISE TEXTURE OVERLAY ── */
      body::before {
        content: '';
        position: fixed;
        inset: 0;
        background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.03'/%3E%3C/svg%3E");
        pointer-events: none;
        z-index: 0;
      }

      /* ── HEADER ── */
      .site-header {
        position: sticky;
        top: 0;
        z-index: 100;
        background: rgba(255,255,255,0.88);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border-bottom: 1px solid var(--border);
        padding: 0 2rem;
        height: 64px;
        display: flex;
        align-items: center;
        justify-content: space-between;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        text-decoration: none;
        color: var(--ink);
      }

      .brand-mark {
        width: 34px;
        height: 34px;
        background: var(--purple);
        color: white;
        border-radius: 10px;
        display: grid;
        place-items: center;
        font-family: var(--serif);
        font-weight: 900;
        font-size: 0.75rem;
        letter-spacing: -0.02em;
        flex-shrink: 0;
      }

      .brand-name {
        font-family: var(--serif);
        font-weight: 700;
        font-size: 1rem;
        color: var(--ink);
      }

      .brand-name span {
        color: var(--purple);
      }

      .header-pill {
        display: flex;
        align-items: center;
        gap: 0.35rem;
        background: var(--purple);
        color: white;
        text-decoration: none;
        padding: 0.45rem 1.1rem;
        border-radius: 100px;
        font-size: 0.82rem;
        font-weight: 600;
        transition: background 0.2s, transform 0.2s;
        white-space: nowrap;
      }

      .header-pill:hover {
        background: var(--purple-dark);
        transform: translateY(-1px);
      }

      /* ── HERO ── */
      .hero {
        position: relative;
        min-height: 520px;
        display: flex;
        align-items: center;
        overflow: hidden;
        padding: 5rem 2rem 4rem;
      }

      /* Purple mesh gradient background */
      .hero::before {
        content: '';
        position: absolute;
        inset: 0;
        background:
          radial-gradient(ellipse 80% 60% at 60% 40%, rgba(124,58,237,0.12) 0%, transparent 70%),
          radial-gradient(ellipse 50% 50% at 20% 80%, rgba(91,33,182,0.08) 0%, transparent 60%),
          radial-gradient(ellipse 40% 40% at 90% 10%, rgba(167,139,250,0.1) 0%, transparent 60%);
        z-index: 0;
      }

      /* Decorative circle */
      .hero::after {
        content: '';
        position: absolute;
        right: -120px;
        top: 50%;
        transform: translateY(-50%);
        width: 500px;
        height: 500px;
        border-radius: 50%;
        border: 1.5px solid rgba(124,58,237,0.12);
        z-index: 0;
      }

      .hero-inner {
        position: relative;
        z-index: 1;
        max-width: 900px;
        margin: 0 auto;
        width: 100%;
      }

      .hero-eyebrow {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        background: var(--purple-mid);
        color: var(--purple-dark);
        padding: 0.35rem 1rem;
        border-radius: 100px;
        font-size: 0.78rem;
        font-weight: 600;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin-bottom: 1.5rem;
        animation: fadeUp 0.6s ease both;
      }

      .hero-eyebrow::before {
        content: '';
        width: 6px;
        height: 6px;
        background: var(--purple);
        border-radius: 50%;
      }

      .hero-title {
        font-family: var(--serif);
        font-size: clamp(2.8rem, 7vw, 5rem);
        font-weight: 900;
        line-height: 1.05;
        letter-spacing: -0.03em;
        color: var(--ink);
        margin-bottom: 1.25rem;
        animation: fadeUp 0.6s 0.1s ease both;
      }

      .hero-title em {
        font-style: italic;
        color: var(--purple);
      }

      .hero-sub {
        font-size: 1.1rem;
        color: var(--ink-soft);
        line-height: 1.7;
        max-width: 520px;
        margin-bottom: 2.25rem;
        animation: fadeUp 0.6s 0.2s ease both;
      }

      .hero-actions {
        display: flex;
        gap: 0.85rem;
        flex-wrap: wrap;
        animation: fadeUp 0.6s 0.3s ease both;
      }

      .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        background: var(--purple);
        color: white;
        text-decoration: none;
        padding: 0.75rem 1.5rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.92rem;
        transition: background 0.2s, transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 20px rgba(124,58,237,0.3);
      }

      .btn-primary:hover {
        background: var(--purple-dark);
        transform: translateY(-2px);
        box-shadow: 0 8px 28px rgba(124,58,237,0.4);
      }

      .btn-ghost {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        background: transparent;
        color: var(--ink-mid);
        text-decoration: none;
        padding: 0.75rem 1.5rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 0.92rem;
        border: 1.5px solid var(--border);
        transition: border-color 0.2s, color 0.2s, transform 0.2s;
      }

      .btn-ghost:hover {
        border-color: var(--purple);
        color: var(--purple);
        transform: translateY(-2px);
      }

      /* Floating stat badges */
      .hero-stats {
        display: flex;
        gap: 1rem;
        margin-top: 3rem;
        flex-wrap: wrap;
        animation: fadeUp 0.6s 0.4s ease both;
      }

      .stat-badge {
        display: flex;
        align-items: center;
        gap: 0.6rem;
        background: white;
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 0.6rem 1rem;
        box-shadow: 0 2px 12px rgba(124,58,237,0.06);
      }

      .stat-icon {
        width: 32px;
        height: 32px;
        background: var(--purple-pale);
        border-radius: 8px;
        display: grid;
        place-items: center;
        font-size: 0.9rem;
        flex-shrink: 0;
      }

      .stat-text strong {
        display: block;
        font-size: 0.88rem;
        font-weight: 700;
        color: var(--ink);
        line-height: 1.2;
      }

      .stat-text span {
        font-size: 0.75rem;
        color: var(--ink-muted);
      }

      /* ── SECTION LAYOUT ── */
      .section {
        max-width: 900px;
        margin: 0 auto;
        padding: 4rem 2rem;
      }

      .section-label {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        font-size: 0.72rem;
        font-weight: 700;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: var(--purple);
        margin-bottom: 0.85rem;
      }

      .section-label::before {
        content: '';
        width: 24px;
        height: 2px;
        background: var(--purple);
        border-radius: 2px;
      }

      .section-title {
        font-family: var(--serif);
        font-size: clamp(1.6rem, 4vw, 2.4rem);
        font-weight: 900;
        color: var(--ink);
        line-height: 1.15;
        letter-spacing: -0.025em;
        margin-bottom: 0.6rem;
      }

      .section-sub {
        font-size: 1rem;
        color: var(--ink-soft);
        line-height: 1.7;
        max-width: 520px;
        margin-bottom: 2.5rem;
      }

      /* ── ARTICLE CARDS ── */
      .articles-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 1.25rem;
      }

      .article-card {
        display: flex;
        flex-direction: column;
        background: white;
        border: 1.5px solid var(--border);
        border-radius: 20px;
        padding: 1.75rem;
        text-decoration: none;
        color: inherit;
        transition: border-color 0.2s, transform 0.2s, box-shadow 0.2s;
        position: relative;
        overflow: hidden;
      }

      .article-card::before {
        content: '';
        position: absolute;
        inset: 0;
        background: radial-gradient(ellipse 80% 60% at 50% 100%, rgba(124,58,237,0.05), transparent);
        opacity: 0;
        transition: opacity 0.3s;
      }

      .article-card:hover {
        border-color: rgba(124,58,237,0.4);
        transform: translateY(-4px);
        box-shadow: 0 16px 40px rgba(124,58,237,0.12);
      }

      .article-card:hover::before { opacity: 1; }

      .card-tag {
        display: inline-flex;
        align-items: center;
        background: var(--purple-pale);
        color: var(--purple-dark);
        font-size: 0.72rem;
        font-weight: 700;
        letter-spacing: 0.07em;
        text-transform: uppercase;
        padding: 0.28rem 0.7rem;
        border-radius: 6px;
        margin-bottom: 1.1rem;
        width: fit-content;
      }

      .card-title {
        font-family: var(--serif);
        font-size: 1.15rem;
        font-weight: 700;
        color: var(--ink);
        line-height: 1.3;
        margin-bottom: 0.6rem;
        letter-spacing: -0.01em;
      }

      .card-desc {
        font-size: 0.88rem;
        color: var(--ink-soft);
        line-height: 1.65;
        flex: 1;
        margin-bottom: 1.25rem;
      }

      .card-footer {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding-top: 1rem;
        border-top: 1px solid var(--border);
      }

      .card-meta {
        font-size: 0.75rem;
        color: var(--ink-muted);
      }

      .card-arrow {
        width: 30px;
        height: 30px;
        background: var(--purple-pale);
        border-radius: 50%;
        display: grid;
        place-items: center;
        color: var(--purple);
        font-size: 0.9rem;
        transition: background 0.2s, transform 0.2s;
        flex-shrink: 0;
      }

      .article-card:hover .card-arrow {
        background: var(--purple);
        color: white;
        transform: translate(2px, -2px);
      }

      /* ── FEATURED (large card) ── */
      .article-card.featured {
        grid-column: 1 / -1;
        flex-direction: row;
        align-items: center;
        gap: 2rem;
        padding: 2.25rem;
        background: linear-gradient(135deg, var(--purple-pale) 0%, white 60%);
      }

      .featured-badge {
        position: absolute;
        top: 1.25rem;
        right: 1.25rem;
        background: var(--purple);
        color: white;
        font-size: 0.68rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        padding: 0.25rem 0.65rem;
        border-radius: 6px;
      }

      .featured-body { flex: 1; }

      .featured-visual {
        width: 180px;
        height: 140px;
        background: linear-gradient(135deg, var(--purple) 0%, var(--purple-dark) 100%);
        border-radius: 16px;
        display: grid;
        place-items: center;
        flex-shrink: 0;
        box-shadow: 0 8px 30px rgba(124,58,237,0.3);
        position: relative;
        overflow: hidden;
      }

      .featured-visual::before {
        content: '';
        position: absolute;
        top: -30px;
        right: -30px;
        width: 100px;
        height: 100px;
        border-radius: 50%;
        background: rgba(255,255,255,0.08);
      }

      .excel-icon {
        font-size: 3.5rem;
        filter: drop-shadow(0 4px 8px rgba(0,0,0,0.2));
        position: relative;
        z-index: 1;
      }

      .featured .card-title {
        font-size: 1.4rem;
        margin-bottom: 0.75rem;
      }

      .featured .card-desc {
        font-size: 0.93rem;
        margin-bottom: 0;
      }

      .featured .card-footer { border-top: none; padding-top: 1.25rem; }

      /* ── DIVIDER ── */
      .divider {
        max-width: 900px;
        margin: 0 auto;
        padding: 0 2rem;
        border: none;
        border-top: 1px solid var(--border);
      }

      /* ── CTA BANNER ── */
      .cta-section {
        padding: 4rem 2rem;
        position: relative;
        overflow: hidden;
      }

      .cta-inner {
        max-width: 900px;
        margin: 0 auto;
        background: var(--ink);
        border-radius: 24px;
        padding: 3rem 2.5rem;
        display: flex;
        align-items: center;
        gap: 2.5rem;
        position: relative;
        overflow: hidden;
      }

      .cta-inner::before {
        content: '';
        position: absolute;
        top: -80px;
        right: -80px;
        width: 300px;
        height: 300px;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(124,58,237,0.4) 0%, transparent 70%);
        pointer-events: none;
      }

      .cta-inner::after {
        content: '';
        position: absolute;
        bottom: -60px;
        left: 30%;
        width: 200px;
        height: 200px;
        border-radius: 50%;
        background: radial-gradient(circle, rgba(124,58,237,0.2) 0%, transparent 70%);
        pointer-events: none;
      }

      .cta-content { flex: 1; position: relative; z-index: 1; }

      .cta-eyebrow {
        font-size: 0.72rem;
        font-weight: 700;
        letter-spacing: 0.12em;
        text-transform: uppercase;
        color: var(--ink-muted);
        margin-bottom: 0.75rem;
      }

      .cta-title {
        font-family: var(--serif);
        font-size: clamp(1.4rem, 3.5vw, 2rem);
        font-weight: 900;
        color: white;
        line-height: 1.2;
        letter-spacing: -0.025em;
        margin-bottom: 0.75rem;
      }

      .cta-title em { font-style: italic; color: #a78bfa; }

      .cta-desc {
        font-size: 0.93rem;
        color: rgba(255,255,255,0.55);
        line-height: 1.7;
        max-width: 420px;
      }

      .cta-actions {
        display: flex;
        flex-direction: column;
        gap: 0.6rem;
        align-items: flex-start;
        position: relative;
        z-index: 1;
        flex-shrink: 0;
      }

      .btn-amber {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        background: #f59e0b;
        color: var(--ink);
        text-decoration: none;
        padding: 0.85rem 1.75rem;
        border-radius: 10px;
        font-weight: 800;
        font-size: 0.95rem;
        white-space: nowrap;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 20px rgba(245,158,11,0.35);
      }

      .btn-amber:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 28px rgba(245,158,11,0.5);
      }

      .cta-note {
        font-size: 0.75rem;
        color: rgba(255,255,255,0.3);
      }

      /* ── FOOTER ── */
      .site-footer {
        border-top: 1px solid var(--border);
        padding: 2rem 2rem 3rem;
        max-width: 900px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 1rem;
        flex-wrap: wrap;
      }

      .footer-brand {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        color: var(--ink);
      }

      .footer-brand-mark {
        width: 28px;
        height: 28px;
        background: var(--purple);
        color: white;
        border-radius: 8px;
        display: grid;
        place-items: center;
        font-family: var(--serif);
        font-weight: 900;
        font-size: 0.65rem;
      }

      .footer-brand-name {
        font-size: 0.88rem;
        font-weight: 600;
        color: var(--ink-mid);
      }

      .footer-right {
        font-size: 0.8rem;
        color: var(--ink-muted);
        display: flex;
        align-items: center;
        gap: 1.25rem;
        flex-wrap: wrap;
      }

      .footer-right a {
        color: var(--purple);
        text-decoration: none;
        font-weight: 500;
      }

      .footer-right a:hover { text-decoration: underline; }

      /* ── ANIMATIONS ── */
      @keyframes fadeUp {
        from { opacity: 0; transform: translateY(20px); }
        to   { opacity: 1; transform: translateY(0); }
      }

      /* ── RESPONSIVE ── */
      @media (max-width: 640px) {
        .site-header { padding: 0 1rem; }
        .hero { padding: 3.5rem 1.25rem 3rem; }
        .hero::after { display: none; }
        .section { padding: 3rem 1.25rem; }
        .article-card.featured { flex-direction: column; }
        .featured-visual { width: 100%; height: 120px; }
        .cta-inner { flex-direction: column; padding: 2rem 1.5rem; }
        .cta-actions { width: 100%; }
        .btn-amber { width: 100%; justify-content: center; }
        .site-footer { padding: 1.5rem 1.25rem 2.5rem; flex-direction: column; align-items: flex-start; }
        .divider { padding: 0 1.25rem; }
      }
    </style>
  </head>

  <body>

    <!-- ── HEADER ── -->
    <header class="site-header">
      <a class="brand" href="https://studentkhata.online?utm_source=blog&utm_medium=header&utm_campaign=homepage">
        <div class="brand-mark">SK</div>
        <span class="brand-name">Student<span>Khata</span></span>
      </a>
      <a class="header-pill" href="https://studentkhata.online/login?utm_source=blog&utm_medium=header&utm_campaign=homepage">
        Try Free — 90 Days →
      </a>
    </header>

    <!-- ── HERO ── -->
    <section class="hero">
      <div class="hero-inner">
        <div class="hero-eyebrow">For Tuition Teachers Across India</div>
        <h1 class="hero-title">
          Free tools to run<br />
          your tuition <em>better.</em>
        </h1>
        <p class="hero-sub">
          Download ready-to-use Excel templates, fee registers, and attendance formats —
          built for private tuition teachers and coaching centers.
        </p>
        <div class="hero-actions">
          <a class="btn-primary" href="#articles">Browse Free Templates ↓</a>
          <a class="btn-ghost" href="https://studentkhata.online?utm_source=blog&utm_medium=hero&utm_campaign=homepage">About StudentKhata</a>
        </div>
        <div class="hero-stats">
          <div class="stat-badge">
            <div class="stat-icon">📊</div>
            <div class="stat-text">
              <strong>Free Templates</strong>
              <span>No signup needed</span>
            </div>
          </div>
          <div class="stat-badge">
            <div class="stat-icon">📱</div>
            <div class="stat-text">
              <strong>Works on Mobile</strong>
              <span>Google Sheets ready</span>
            </div>
          </div>
          <div class="stat-badge">
            <div class="stat-icon">🇮🇳</div>
            <div class="stat-text">
              <strong>Made for India</strong>
              <span>₹ format, Hindi-friendly</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <hr class="divider" />

    <!-- ── ARTICLES ── -->
    <section class="section" id="articles">
      <p class="section-label">Free Downloads</p>
      <h2 class="section-title">Templates &amp; Guides</h2>
      <p class="section-sub">
        Practical resources you can download and use today — no email required.
      </p>

      <div class="articles-grid">

        <!-- Featured card -->
        <a class="article-card featured" href="/free-tuition-fee-register-format-excel-download">
          <span class="featured-badge">⭐ Featured</span>
          <div class="featured-body">
            <div class="card-tag">Excel Template</div>
            <h2 class="card-title">Free Tuition Fee Register Format in Excel</h2>
            <p class="card-desc">
              Track monthly payments, mark pending dues, and manage student records —
              all in a pre-built Excel sheet that does the math for you. Works on Google Sheets too.
            </p>
            <div class="card-footer">
              <span class="card-meta">⏱ 7 min read &nbsp;·&nbsp; 📥 Free download</span>
              <div class="card-arrow">→</div>
            </div>
          </div>
          <div class="featured-visual">
            <span class="excel-icon">📊</span>
          </div>
        </a>

        <!-- Placeholder card — coming soon style -->
        <div class="article-card" style="opacity:0.5; cursor: default; pointer-events: none;">
          <div class="card-tag">Coming Soon</div>
          <h2 class="card-title">Free Attendance Register Format in Excel</h2>
          <p class="card-desc">
            A simple daily attendance sheet for tuition teachers. Mark P, A, or L for each student.
          </p>
          <div class="card-footer">
            <span class="card-meta">Coming soon</span>
            <div class="card-arrow">→</div>
          </div>
        </div>

        <div class="article-card" style="opacity:0.5; cursor: default; pointer-events: none;">
          <div class="card-tag">Coming Soon</div>
          <h2 class="card-title">How to Send Fee Reminders on WhatsApp</h2>
          <p class="card-desc">
            Copy-paste message templates that are polite, effective, and don't feel awkward to send.
          </p>
          <div class="card-footer">
            <span class="card-meta">Coming soon</span>
            <div class="card-arrow">→</div>
          </div>
        </div>

      </div>
    </section>

    <!-- ── CTA ── -->
    <section class="cta-section">
      <div class="cta-inner">
        <div class="cta-content">
          <p class="cta-eyebrow">Beyond Excel</p>
          <h2 class="cta-title">
            Ready to stop doing<br />
            fee admin <em>manually?</em>
          </h2>
          <p class="cta-desc">
            StudentKhata handles fees, attendance, pending dues, WhatsApp reminders,
            and PDF receipts automatically — so you can focus on teaching.
          </p>
        </div>
        <div class="cta-actions">
          <a class="btn-amber" href="https://studentkhata.online/login?utm_source=blog_home&utm_medium=cta&utm_campaign=homepage">
            Start Free — 90 Days →
          </a>
          <span class="cta-note">No credit card. No commitment.</span>
        </div>
      </div>
    </section>

    <!-- ── FOOTER ── -->
    <footer class="site-footer">
      <a class="footer-brand" href="https://studentkhata.online">
        <div class="footer-brand-mark">SK</div>
        <span class="footer-brand-name">StudentKhata Blog</span>
      </a>
      <div class="footer-right">
        <span>© {new Date().getFullYear()} StudentKhata</span>
        <a href="https://studentkhata.online">studentkhata.online</a>
      </div>
    </footer>

  </body>
</html>
